
result_dir: "temp/"
tcga_dir: "data/tcga/"
reactome_dir: "data/reactome/"

sim_params:
    data_params:
        snr: [99.0]
        true_K: [300]
        reps: 1
    matfac_params:
        max_epochs: 500
        lr: 0.07
        lambda_layer: 5.0
        lambda_X: 0.001
        lambda_Y: 0.2
        used_K: [300]
 
